group = "com.anesfz"
version = "1.0.0"

plugins {
    id("com.lagradost.cloudstream") version "1.0.0"
}

cloudstream {
    name = "ApexCloudStream"
    author = "Anes + Grok"
    description = "The absolute best CloudStream repo 2026 — watch ANYTHING (movies, series, anime). Fast & working."
    language = "multi"
    status = com.lagradost.cloudstream.Status.Working
}
